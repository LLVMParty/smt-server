[package]
name = "smt-wire"
version.workspace = true
edition.workspace = true
description = "Rust codecs, views, validation, and builders for the SMT v1 wire format"
license.workspace = true

[lib]
name = "smt_wire"
path = "src/lib.rs"
